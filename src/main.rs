use std::{fs, path};
use ini::ini;

fn ReadConfigFile(ConfigPath:&str) {
    
    let ConfigFile = ini!(ConfigFile);

}

fn ReadDirectory(path:&str) {
    
}

fn main() {
    println!("Hello, world!");
}
